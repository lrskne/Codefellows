<h1>Listing topics</h1>

<table>
  <tr>
    <th>Title</th>
    <!-- lbe removed this line <th>Description</th> -->
    <th></th>
    <th></th>
    <th></th>
  </tr>
<!-- Older code lbe -->
<!--<% @topics.each do |topic| %>-->
<!--  <tr>-->
<!--    <td><%= topic.title %></td>-->
<!--    <td><%= topic.description %></td>-->
<!--    <td><%= link_to 'Show', topic %></td>-->
<!--    <td><%= link_to 'Edit', edit_topic_path(topic) %></td>-->
<!--    <td><%= link_to 'Destroy', topic, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
<!--  </tr>-->
<!--<% end %>-->
<% @topics.each do |topic| %>
    <tr>
      <!-- lbenote removed then added next line...<td><%= topic.title %></td> -->
      <td><%= link_to topic.title, topic %></td>
      <!-- lbe removed <td><%= topic.description %></td> -->
      <td><%= pluralize(topic.votes.count, "vote") %></td>
      <td><%= button_to '+1', topic_votes_path(topic_id: topic.id), method: :post %></td>
      <!-- added button to subtract a vote for unsure people -->
      <td><%= button_to '-1ooo', [topic, topic.votes.last] , method: :delete %></td>
      <!--<td><%= link_to 'Show', topic %></td>-->
      <!--<td><%= link_to 'Edit', edit_topic_path(topic) %></td>-->
      <!--<td><%= link_to 'Destroy', topic, confirm: 'Are you sure?', method: :delete %></td>-->
      <td><%= link_to 'Delete', topic, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>

<br />

<%= link_to 'New Topic', new_topic_path %>
